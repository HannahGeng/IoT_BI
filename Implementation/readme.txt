1. Install the centos virtual machine image（username/password: root/123456）

2. Connect MySQL and create database and tables（username/password: root/root123）

3. Access the consul platform add some configurations, use ip:8500 to access

4. Run the backend server

5. Run the front-end server: install nodes and yarn -> yarn install -> yarn serve
